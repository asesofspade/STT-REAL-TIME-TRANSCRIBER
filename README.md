# STT-REAL-TIME-TRANSCRIBER
It access your mic from Google Collab, it listens when you start recording, but only when you talk, after u end talking, it will record like, recording1, recording2, and then, when u click transcribe all button, it will start transcribing every recording into a big speech.
